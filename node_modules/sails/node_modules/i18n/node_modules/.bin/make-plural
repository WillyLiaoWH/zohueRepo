#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../make-plural/bin/make-plural" "$@"
  ret=$?
else 
  node  "$basedir/../make-plural/bin/make-plural" "$@"
  ret=$?
fi
exit $ret
